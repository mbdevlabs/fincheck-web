version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - corepack enable
      - corepack prepare pnpm@10 --activate
      - pnpm install --frozen-lockfile

  pre_build:
    commands:
      - pnpm ci:lint
      - pnpm typecheck

  build:
    commands:
      - pnpm build

  post_build:
    commands:
      - aws s3 sync dist/ s3://mb-fincheck --delete
      - aws cloudfront create-invalidation --distribution-id E1OKGBWHW86H6X --paths "/*"

cache:
  paths:
    - node_modules/.pnpm-store/**/*
